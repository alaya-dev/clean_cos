# Rotation du token Meta

Créer le nouveau token côté propriétaire, tester en dataset de test, saisir via l’UI chiffrée avec confirmation, vérifier masquage/audit, puis révoquer l’ancien côté Meta. Ne jamais mettre le token dans `.env`, logs ou ticket.
