# Smoke test post-déploiement

Vérifier `/up`, `/api/health/live`, `/api/health/ready`, homepage, produit, panier, checkout synthétique, login admin, worker/scheduler, stockage image et Sentry redigé. Contrôler cache/Redis et les jobs échoués. Meta live reste exclu sans checklist différée approuvée.
