# Révoquer les sessions

Désactiver ou modifier le compte via le flux autorisé, invalider les sessions Redis du compte selon le mécanisme existant, vérifier qu’une API protégée renvoie 401/403, puis consigner l’incident sans PII dans l’audit/Sentry.
