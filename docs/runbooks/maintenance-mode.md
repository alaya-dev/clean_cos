# Mode maintenance

Vérifier la fenêtre et les commandes en cours, activer `php artisan down --render=errors::503`, conserver accès de déploiement autorisé, puis `php artisan up` et smoke tests. Ne pas interrompre une transaction checkout sans analyse.
